---
/**
 * (Emekli) Eski "pixel twinkle" katmanı. Yeni tasarım dilinde arka plan
 * atmosferi global.css'teki body::before/::after (mavi glow + noise) ile
 * sağlanıyor, bu yüzden bileşen artık hiçbir şey render etmiyor.
 * Sayfalardaki <PixelField /> çağrıları uyumluluk için zararsızca duruyor.
 */
interface Props {
  count?: number;
}
---
